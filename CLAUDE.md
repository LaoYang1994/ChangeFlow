@AGENTS.md

# Claude Code

- Use plan mode when designing a non-trivial change to this plugin.
- Use TodoWrite to track multi-step work (adding a command, a release).
- `templates/` holds the stubs the plugin ships to *other* repos. This repo's
  own durable docs are at the root `AGENTS.md`/`CLAUDE.md` and under `docs/`.
- When compacting, preserve the active change (if any) and what's been pushed.
