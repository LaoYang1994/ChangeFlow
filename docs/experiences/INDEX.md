# Experiences index

Reusable lessons and pitfalls. One line per experience — keep it scannable so
an agent can find the right lesson without opening every file.

- [Codex rescue subagent kills its child](workflow/codex-rescue-kills-child.md) — long Codex jobs die when dispatched via the rescue subagent; drive `codex exec` directly instead
